import { ShiftTypeFilterPipe } from './shift-type-filter.pipe';

describe('ShiftTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
